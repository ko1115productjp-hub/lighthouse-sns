# ==============================================================================
# Lighthouse Production Environment Variables
# ==============================================================================
# このファイルは本番環境（Vercel）で設定すべき環境変数の一覧です。
# Vercel Dashboard の "Settings" → "Environment Variables" で設定してください。
# ==============================================================================

# ------------------------------------------------------------------------------
# Database (Supabase)
# ------------------------------------------------------------------------------
# Supabase Dashboard → Settings → Database → Connection string (Pooler)
# 重要: Pooler接続（ポート6543）を使用してください。サーバーレス環境で必須です。
DATABASE_URL=postgresql+asyncpg://postgres:[YOUR_PASSWORD]@db.xxx.supabase.co:6543/postgres?pgbouncer=true&ssl=require

# ------------------------------------------------------------------------------
# Redis (Upstash)
# ------------------------------------------------------------------------------
# Upstash Console → Your Database → Details
REDIS_URL=https://xxx.upstash.io
REDIS_TOKEN=AxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxQ

# ------------------------------------------------------------------------------
# Security
# ------------------------------------------------------------------------------
# 以下のコマンドで生成: openssl rand -hex 32
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# ------------------------------------------------------------------------------
# CORS
# ------------------------------------------------------------------------------
# フロントエンドのURLを配列形式で指定（JSON形式）
CORS_ORIGINS=["https://lighthouse-frontend.vercel.app"]

# ------------------------------------------------------------------------------
# Application Settings
# ------------------------------------------------------------------------------
APP_NAME=Lighthouse
DEBUG=False
API_VERSION=v1

# ------------------------------------------------------------------------------
# AI - OpenAI
# ------------------------------------------------------------------------------
# OpenAI Platform → API Keys で取得
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ------------------------------------------------------------------------------
# Web Search - Serper API (Optional)
# ------------------------------------------------------------------------------
# Serper.dev で取得（独自性チェック・盗作検出に使用）
SERPER_API_KEY=your-serper-api-key

# ------------------------------------------------------------------------------
# Google APIs (Optional)
# ------------------------------------------------------------------------------
# Google Cloud Console → APIs & Services → Credentials
GOOGLE_PLACES_API_KEY=your-google-places-api-key
GOOGLE_BOOKS_API_KEY=your-google-books-api-key

# ------------------------------------------------------------------------------
# TMDb API (Optional - Phase 2+)
# ------------------------------------------------------------------------------
# The Movie Database (TMDb) で取得
TMDB_API_KEY=your-tmdb-api-key
TMDB_ACCESS_TOKEN=your-tmdb-access-token

# ------------------------------------------------------------------------------
# Originality Check Settings
# ------------------------------------------------------------------------------
# 独自性スコアの閾値（60未満はPrivate投稿になる）
ORIGINALITY_THRESHOLD=60
# 類似度の閾値（0.85以上で既存コンテンツと類似と判定）
SIMILARITY_THRESHOLD=0.85

# ------------------------------------------------------------------------------
# OAuth - Google
# ------------------------------------------------------------------------------
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# バックエンドの実際のURLに置き換えてください
GOOGLE_REDIRECT_URI=https://lighthouse-backend.vercel.app/api/v1/auth/google/callback

# ------------------------------------------------------------------------------
# OAuth - LINE
# ------------------------------------------------------------------------------
# LINE Developers Console → Your Channel → Basic Settings
LINE_CHANNEL_ID=your-line-channel-id
LINE_CHANNEL_SECRET=your-line-channel-secret
# バックエンドの実際のURLに置き換えてください
LINE_REDIRECT_URI=https://lighthouse-backend.vercel.app/api/v1/auth/line/callback

# ------------------------------------------------------------------------------
# AWS (Phase 2+ - 画像アップロード機能)
# ------------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET_NAME=

# ------------------------------------------------------------------------------
# GCP (AWS の代替)
# ------------------------------------------------------------------------------
GOOGLE_APPLICATION_CREDENTIALS=
GCS_BUCKET_NAME=
